# Platform tests

Cross-package integration, tenant-isolation, workflow-replay, and browser tests belong here. Package-local tests cover the foundation contracts first. Real customer or prospect data must never be used in fixtures.
